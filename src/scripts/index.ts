// Re-export all animation utilities
// Usage: import { initScrollAnimations, animateCounters } from '../scripts';

export { initScrollAnimations, setupScrollAnimations } from './scroll-animate';
export { animateCounters, setupCounterAnimations } from './counter';
